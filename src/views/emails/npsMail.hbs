<style>
    .container {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        align-content: center;
        width: 800px;
    }

    .answers {
        display: flex;
        justify-content: space-around;
        width: 350px;
    }

    .answers__values {
        width: 15px;
        padding: 10px;
        margin: 1.5px;
        border-radius: 6px;
        background: #8257;
        color: #fff;
        text-align: center;
        text-decoration: none;
    }

    .level {
        display: flex;
        margin: 10px;
        justify-content: space-between;
        width: 350px;
    }
</style>

<main class="container">
    <p>Olá, <strong>{{name}}</strong>! Tudo bem?</p>

    <h3>{{title}}</h3>
    <br />
    <strong>{{description}}</strong>

    <section class="level">
        <span>Pouco provável</span>
        <span>Muito provável</span>
    </section>

    <section class="answers">
        <a class="answers__values" href="{{link}}/1?u={{user_id}}">1</a>
        <a class="answers__values" href="{{link}}/2?u={{user_id}}">2</a>
        <a class="answers__values" href="{{link}}/3?u={{user_id}}">3</a>
        <a class="answers__values" href="{{link}}/4?u={{user_id}}">4</a>
        <a class="answers__values" href="{{link}}/5?u={{user_id}}">5</a>
        <a class="answers__values" href="{{link}}/6?u={{user_id}}">6</a>
        <a class="answers__values" href="{{link}}/7?u={{user_id}}">7</a>
        <a class="answers__values" href="{{link}}/8?u={{user_id}}">8</a>
        <a class="answers__values" href="{{link}}/9?u={{user_id}}">9</a>
        <a class="answers__values" href="{{link}}/10?u={{user_id}}">10</a>
    </section>

    <br />
    <br />

    <p>Sua opinião é muito importante para nós e nos ajuda a melhorar cada vez mais!</p>

    <h4>Equipe | NLW4</h4>
</main>